# HTML Resizable tables

Project focused on doing a table with resizable column.

The idea is:
- Resize a single column without affect the other ones
- Store the resized columns to persis the user preference
